% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/EvaluateClusters.R
\name{EvaluateClusters}
\alias{EvaluateClusters}
\title{Evaluate Cluster Quality from PTM Data}
\usage{
EvaluateClusters(
  clusterlist,
  tbl.sc,
  data.type = c("ratio", "intensity", "count"),
  index.mode = c("density", "size"),
  use.slope = TRUE,
  ratio.col.pattern = NULL,
  verbose = TRUE
)
}
\arguments{
\item{clusterlist}{A named or unnamed list of clusters as returned by
\code{\link[=MakeClusterList]{MakeClusterList()}}.  Each element must be a character vector of gene /
PTM names \strong{or} a data frame with a column named \code{PTMnames} (the format
produced by \code{MakeClusterList}).}

\item{tbl.sc}{Numeric data matrix with PTMs / genes as row names and samples
as column names.  This is the same table passed to \code{\link[=MakeClusterList]{MakeClusterList()}}.}

\item{data.type}{Character string specifying the nature of the input data.
One of:
\describe{
\item{\code{"ratio"} (default)}{TMT log2 ratios; values may be negative.
Absolute values are used internally for all diagnostic calculations so
that signal magnitude is assessed without sign cancellation.}
\item{\code{"intensity"}}{Non-negative raw TMT intensities or peptide
abundance values.  No absolute-value transformation is applied.}
\item{\code{"count"}}{Non-negative peptide count data (original
\code{clust.eval.1} behaviour).  Treated identically to \code{"intensity"}.}
}}

\item{index.mode}{Character string selecting the index formula.  One of
\code{"density"} (default, recommended) or \code{"size"}.  See the \emph{Index formula}
section above.}

\item{use.slope}{Logical.  If \code{TRUE} (default), apply the slope-based
outlier filter (see \emph{Slope filter} section).  Set to \code{FALSE} for
unordered cohort data such as BRCA TMT proteomics, where sample order
is arbitrary.}

\item{ratio.col.pattern}{Character string or \code{NULL} (default).  A regular
expression passed to \code{\link[base:grep]{base::grep()}} to identify and remove ratio columns
before computing diagnostic statistics.  For example, LINCS data may
contain columns named \code{"H1_to_control"} whose name encodes a ratio
relationship; passing \code{"to"} removes these columns before evaluation.
Set to \code{NULL} (default) to skip this step.}

\item{verbose}{Logical.  If \code{TRUE} (default), print progress messages for
each cluster.  Set to \code{FALSE} to suppress output in batch workflows.}
}
\value{
A data frame sorted in descending order of \code{Index}, then ascending
\code{percent.NA}.  Columns are:
\describe{
\item{\code{Group}}{Integer cluster index (1-based, matching \code{clusterlist}
position).}
\item{\code{no.genes}}{Number of genes / PTMs in the cluster.}
\item{\code{culled.by.slope}}{Number of rows flagged by the slope filter (0
when \code{use.slope = FALSE}).}
\item{\code{percent.singlesamplegenes}}{Percentage of genes / PTMs detected
in only one sample.}
\item{\code{no.samples}}{Number of samples that contain at least one
non-missing value for any gene in the cluster.}
\item{\code{percent.singlegenesamples}}{Percentage of samples that contain
a non-missing value for only one gene in the cluster.}
\item{\code{total.signal}}{Sum of absolute signal values across the cluster
sub-table (after NA removal and, when applicable, absolute-value
transformation).}
\item{\code{percent.NA}}{Percentage of missing values in the cluster
sub-table restricted to samples with at least one non-missing value.}
\item{\code{intensity}}{Total signal after scaling by the missing-value
fraction (\code{total.signal * (1 - percent.NA / 100)}).  Only included
when \code{index.mode = "density"}.}
\item{\code{Index}}{Composite quality index (see \emph{Index formula} section).
Higher values indicate better clusters.}
}
}
\description{
Computes a set of diagnostic statistics and a composite quality index for
each cluster in a cluster list, using the original PTM data matrix as the
reference.  The function consolidates four previous evaluation functions
(\code{clust.eval.1}, \code{nbclust.eval}, \code{lincsclust.eval}, and \code{clust.eval}) into
a single interface controlled by three arguments: \code{data.type},
\code{index.mode}, and \code{use.slope}.
}
\section{Index formula}{

Two index modes are available, selected with \code{index.mode}:

\describe{
\item{\code{"size"} (original \code{nbclust.eval} / \code{clust.eval})}{
Rewards large, information-dense clusters.
\deqn{
      \text{Index} = \text{intensity}
        \times (1 + \text{realsamples})
        \times (1 + \text{cleargenes})
        \;/\; (1 + \text{percent.NA})
    }
}
\item{\code{"density"} (original \code{lincsclust.eval}; recommended default)}{
Rewards data density per gene.  Compact, information-rich clusters
score higher than large but diffuse ones.
\deqn{
      \text{Index} =
        (1 + \text{realsamples})
        \times (1 + \text{cleargenes})
        \;/\; ((1 + \text{percent.NA}) \times \text{no.genes})
    }
}
}

where:
\itemize{
\item \code{intensity} = total signal after removing the NA fraction
\item \code{realsamples} = samples that are not single-gene samples
\item \code{cleargenes} = genes not culled by the slope filter
\item \code{percent.NA} = percentage of missing values in the cluster sub-table
}
}

\section{Slope filter}{

When \code{use.slope = TRUE}, the cluster sub-table is re-ordered so that columns
(samples) are sorted by decreasing total signal and rows (genes/PTMs) are
sorted by decreasing total signal.  A linear model is then fit to each row
against its column rank.  Rows with a positive or undefined slope are flagged
as "bad slope" and counted in \code{culled.by.slope}.  These rows do not
contribute to the index because a positive slope (signal increasing with
decreasing sample rank) is inconsistent with the pattern expected for a
coherent cluster.  Set \code{use.slope = FALSE} for unordered cohort data (e.g.
BRCA TMT) where sample order is arbitrary and a slope filter is
not meaningful.
}

\examples{
# Typical usage with TMT ratio data and default density index:
cl <- MakeClusterList(ex_tiny_ptm_table)
eval_df <- EvaluateClusters(cl[[1]], ex_tiny_ptm_table, data.type = "ratio")
head(eval_df)

# Unordered cohort data (e.g. BRCA): disable slope filter
eval_brca <- EvaluateClusters(
  brca_clusters, brca_tbl,
  data.type  = "ratio",
  use.slope  = FALSE,
  index.mode = "density"
)

# LINCS data with ratio columns to strip, size-based index:
eval_lincs <- EvaluateClusters(
  lincs_clusters, lincs_tbl,
  data.type          = "ratio",
  index.mode         = "size",
  use.slope          = TRUE,
  ratio.col.pattern  = "to"
)
}
\seealso{
\code{\link[=MakeClusterList]{MakeClusterList()}} for generating the cluster list input.
}
